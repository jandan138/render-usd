# 清理与新文件渲染计划

我将清理之前的测试结果，并对指定的新 USD 文件进行渲染。

## 1. 清理旧数据
*   删除 `output_test` 和 `output_test_v2` 目录。

## 2. 渲染新文件
*   使用 `single` 命令渲染 `/cpfs/shared/simulation/zhuzihou/assets/GRScenes100-for-render/GRScenes_assets/bed/0a85b986de35ccfdec7c686d791fd747/0a85b986de35ccfdec7c686d791fd747.usd`。
*   将结果保存到 `output_new`（或其他指定目录）。

## 3. 验证
*   检查新输出目录中的图片是否正常生成且非全黑。
