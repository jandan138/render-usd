# 安装与测试计划

为了满足您对指定 USD 文件 (`examples/0a85b986de35ccfdec7c686d791fd747/0a85b986de35ccfdec7c686d791fd747.usd`) 的测试需求，我需要扩展 CLI 功能以支持单文件渲染，并执行安装步骤。

## 1. 安装项目
在当前环境下安装 `render-usd` 包及其依赖：

```bash
pip install -e .
```

## 2. 扩展 CLI 功能
目前的 CLI 主要针对特定数据集结构（GRScenes/GRScenes100）。我将在 `src/render_usd/cli.py` 中添加一个新的子命令 `single`，专门用于渲染单个 USD 文件。

**新命令用法示例：**
```bash
python -m render_usd.cli single \
    --usd_path /path/to/file.usd \
    --output_dir ./output_test
```

**修改计划：**
*   修改 `src/render_usd/cli.py`:
    *   注册 `single` 子命令。
    *   添加参数 `--usd_path` 和 `--output_dir`。
    *   调用 `renderer.render_thumbnail_wo_bg` 处理单个文件。

## 3. 执行测试
使用新命令对您提供的文件进行测试：

```bash
python -m render_usd.cli single \
    --usd_path /cpfs/shared/simulation/zhuzihou/dev/render-usd/examples/0a85b986de35ccfdec7c686d791fd747/0a85b986de35ccfdec7c686d791fd747.usd \
    --output_dir /cpfs/shared/simulation/zhuzihou/dev/render-usd/examples/output_test
```

## 4. 验证结果
检查输出目录是否有生成的缩略图。
