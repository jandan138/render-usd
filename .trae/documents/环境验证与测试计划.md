# 验证计划

既然环境创建已完成，我将按照以下步骤进行验证：

## 1. 安装项目包
激活 `render-usd` 环境，并以编辑模式安装当前项目，确保 `render_usd` 命令可用。

```bash
source ./miniconda/bin/activate
conda activate render-usd
pip install -e .
```

## 2. 执行渲染测试
使用之前添加的 `single` 命令渲染指定的 USD 文件。

```bash
python -m render_usd.cli single \
    --usd_path /cpfs/shared/simulation/zhuzihou/dev/render-usd/examples/0a85b986de35ccfdec7c686d791fd747/0a85b986de35ccfdec7c686d791fd747.usd \
    --output_dir ./output_test
```

## 3. 验证输出结果
检查 `./output_test` 目录下是否生成了预期的图片文件（如 `*_bbox2d.png` 或 `*.png`）。

```bash
ls -R ./output_test
```
